/**
 * T3Team's compatibility export for the reusable typed tree utilities.
 *
 * The implementation lives with the independent tools/scripts contracts; this file keeps the
 * existing `@t3team/sdk` type surface stable for workflow and recipe consumers.
 */

export type { ScriptTreeFromRecord } from "@runbook/scripts";
export type { ToolTreeFromRefs } from "@runbook/tools";
